<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.xiaoyuu.inwust.dao.CourseInfoMapper">
    <resultMap id="BaseResultMap" type="me.xiaoyuu.inwust.model.CourseInfo">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="course_code" jdbcType="VARCHAR" property="courseCode"/>
        <id column="course_credit" jdbcType="VARCHAR" property="courseCredit"/>
        <id column="course_type" jdbcType="INTEGER" property="courseType"/>
        <id column="teacher_name" jdbcType="VARCHAR" property="teacherName"/>
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="course_name" jdbcType="VARCHAR" property="courseName"/>
        <result column="course_hour" jdbcType="INTEGER" property="courseHour"/>
        <result column="course_type_name" jdbcType="VARCHAR" property="courseTypeName"/>
        <result column="unit_name" jdbcType="VARCHAR" property="unitName"/>
        <result column="average_grade" jdbcType="VARCHAR" property="averageGrade"/>
        <result column="grade_count" jdbcType="INTEGER" property="gradeCount"/>
        <result column="pass_percent" jdbcType="VARCHAR" property="passPercent"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <select id="selectByTeacherNameAndCourseName" resultMap="BaseResultMap">
        select id,
               course_code,
               course_name,
               course_credit,
               course_hour,
               course_type,
               course_type_name,
               teacher_name,
               unit_name,
               average_grade,
               grade_count,
               pass_percent,
               create_time,
               update_time
        from inwust.course_info
        where teacher_name like concat('%', #{teacherName}, '%')
          and course_name like concat('%', #{courseName}, '%')
    </select>

    <select id="selectNumByTeacherNameAndCourseName" resultType="java.lang.Integer">
        select count(*)
        from inwust.course_info
        where teacher_name like concat('%', #{teacherName}, '%')
          and course_name like concat('%', #{courseName}, '%')
    </select>
    <select id="selectByCollegeCode" resultMap="BaseResultMap">
        select id,
               course_code,
               course_name,
               course_credit,
               course_hour,
               course_type,
               course_type_name,
               teacher_name,
               unit_name,
               average_grade,
               grade_count,
               pass_percent,
               create_time,
               update_time
        from inwust.course_info
        where course_code like concat(#{collegeCode}, '%')
    </select>

    <select id="selectByCourseName" resultMap="BaseResultMap">
        select id,
               course_code,
               course_name,
               course_credit,
               course_hour,
               course_type,
               course_type_name,
               teacher_name,
               unit_name,
               average_grade,
               grade_count,
               pass_percent,
               create_time,
               update_time
        from inwust.course_info
        where course_name like concat('%', #{keyword}, '%')
    </select>

    <select id="selectByTeacherName" resultMap="BaseResultMap">
        select id,
               course_code,
               course_name,
               course_credit,
               course_hour,
               course_type,
               course_type_name,
               teacher_name,
               unit_name,
               average_grade,
               grade_count,
               pass_percent,
               create_time,
               update_time
        from inwust.course_info
        where teacher_name like concat('%', #{keyword}, '%')
    </select>

    <update id="updateCourseCredit" parameterType="java.lang.String">
        update inwust.course_info
        set course_credit=#{courseCredit}
        where
    </update>

</mapper>